TODO list (organized in descending priority)
Add browser action popup with statistics and current page whitelisting settings
Add options page
Add option to detect clicks within a certain rectangular area of media elements
    Set it per known media element type or the unknown type
    Have option to set it per website
Add website whitelisting (with regex capability?)
Use chrome.tabs.executeScript instead of declaring content scripts in manifest.json(?)
    Selectively inject scripts into tabs depending on whitelist
    May allow for earlier script injection with webNavigation events
Add welcome page on extension update and first time installation to inform the user about how to use my extension
Add debug logging
Cleanup frame_script.js
    Use ES6-style class declarations and add element mutated callback
        Inherit from BaseDelegate
Add option to disable media buffering(?)
